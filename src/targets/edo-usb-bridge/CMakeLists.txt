# Generated by ModuleTarget.py
# Remove a "#" from the line below to stop generating this file every time you call CoreWorkspace generate
# TARGET MODULE usb

PROJECT( usb )
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

#set(BOOTLOADER_SIZE 0x4000)
SET( BOOTLOADER_SIZE 16384 )
SET( CONFIGURATION_SIZE 2048 )

FIND_PACKAGE( CORE_BUILD CONFIG REQUIRED )

ADD_DEFINITIONS(-DOVERRIDE_MODULE_ID=0x00 )
ADD_DEFINITIONS(-DOVERRIDE_MODULE_NAME="bridge" )

INCLUDE ( CoreTarget NO_POLICY_SCOPE )
SET( PROJECT_INCLUDE_DIRECTORIES
)

SET( PROJECT_SOURCES
  main.cpp
)

ADD_DEFINITIONS(-DCORE_IS_BOOTLOADER_BRIDGE=1)
ADD_DEFINITIONS(-DSERIAL_SHELL)
#ADD_DEFINITIONS(-DCORE_DEFAULT_FORWARDING_RULE=false)

core_target_module(
  IS_BRIDGE
  MODULE usb
  OS_VERSION CHIBIOS_16
  PACKAGES
    led
    common_msgs
)
